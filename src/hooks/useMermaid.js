import { useState } from "react";

const useMermaid = () => {
  const [valid, setValid] = useState(false);
  const [validationProgress, setValidattionProgress] = useState(false);
  const [validationStack, setValidationStack] = useState([]);
  return {};
};

export default useMermaid;
